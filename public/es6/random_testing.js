let currentTemp = 22;

let targetTempC;

let room1 = 'living room';
let room2 = room1;
room1 += ' magic';

let unknown = null;

const obj = {};

// es6 feature: object destructuring
function say(thing) {
  // es6 feature: template strings
  // note that quotes below are backticks (`), not single quotes (')
  console.log(`${thing}`);
}

say( targetTempC==unknown );

say(room1);
say(room2);


//console.log(`${currentTemp}`);
//console.log(`${currentTemp}`);
